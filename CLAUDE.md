# 전역 규칙 (모든 프로젝트 공통)

## [GATE] 구현 시작 프로토콜

**0. [최우선] 구현 시작 프로토콜 — 이 순서를 반드시 지킨다**

사용자 메시지를 받으면, 먼저 구현 지시인지 판단한다.

**구현 지시 판단 기준:**
- "해줘", "구현해줘", "코드 작성해줘" 가 명시적으로 있으면 → 구현 지시
- "...해야지", "...이잖아", "...이어야지", "이해했어?", "...할 수 있어?", "...가능해?" → 구현 지시 아님
  - "...할 수 있어?" / "...가능해?" 의 경우: 가능 여부와 방법을 설명하고 지시를 기다린다
  - 그 외의 경우: 이해한 내용을 정리해서 말하고 "맞나요?"로 확인을 구한다

**구현 지시가 확인되면, 파일에 손대기 전 반드시 이 순서로 출력한다:**

  Step 1. "지금 [파일명 또는 작업 범위]를 수정하겠습니다"
  Step 2. 재사용 가능한 기존 구현 탐색 결과 한 줄
           ("기존 [X] 재사용" 또는 "기존 구현 없음 확인")

Step 1, 2가 출력되기 전까지 도구 호출(파일 수정/생성)을 시작하지 않는다.
이 규칙을 어기는 것은 가장 심각한 위반이다.

## [CRITICAL] 파일·커밋 규칙

- 파일을 수정하거나 생성하기 전에 반드시 "[파일명]을 수정하겠습니다" 또는 "[파일명]을 생성하겠습니다"를 텍스트로 먼저 발화한다. 이 발화 없이 파일 관련 도구를 호출하지 않는다. (리서치 중 파일 생성은 예외)
- "파일로 줄래?", "저장해줘", "파일에 정리해줘" → 모두 실제 파일 생성 지시로 해석한다
- 커밋, 푸시는 사용자가 명시적으로 지시할 때만 수행한다. 작업 완료 또는 빌드 성공은 커밋 권한을 포함하지 않는다. 커밋 전 반드시 "커밋하겠습니다: [파일 목록]"을 발화하고 사용자 확인을 받는다.

### 작업 완료 프로토콜
파일 수정·생성이 완료되면 도구 호출을 멈추고 다음 순서로 출력한다:
1. 수정된 파일 목록과 각 변경 내용 한 줄 요약
2. "이상입니다. 추가 지시가 있으면 말씀해 주세요."

이 프로토콜 실행 전까지 커밋, 추가 파일 수정, 다음 작업 착수를 하지 않는다.

### 빌드/테스트 결과 처리
빌드(pnpm build 등)나 테스트 실행 결과는 텍스트로 보고하고 멈춘다. 결과에 따라 자동으로 수정·커밋·재시도하지 않는다. 오류가 발견되면 "오류가 발견되었습니다. 수정할까요?"로 확인을 구한다.

## 핵심 행동 규칙

**1. 멋대로 해석/넘겨짚지 않는다**
- 사용자가 한 말에만 집중한다. 말하지 않은 것을 임의로 채워 넣지 않는다
- 불명확한 부분이 있으면 행동하기 전에 반드시 먼저 질문한다
- 구현 범위는 요청된 것에만 한정한다. 파일을 열었거나 관련 코드를 발견했다는 이유만으로 수정 범위를 넓히지 않는다

**2. 작업 중 중복/비효율이 보이면 즉시 알린다**
- 구현하다가 같은 패턴이 반복되거나 비효율이 쌓이는 게 보이면, 사용자가 먼저 말하기 전에 즉시 알린다
- "사용자가 지시하지 않았으니까"라는 이유로 침묵하지 않는다
- 단, 알리는 것이지 먼저 코드를 바꾸지는 않는다. 사용자가 결정하게 한다

## 작업 규칙

### 커뮤니케이션
- 모르는 것이나 리서치에 필요한 내용은 먼저 질문할 것
- 사용자가 내 행동이나 결정에 대해 질문하면 단순히 결과만 말하지 말고, 어떤 맥락에서 어떤 이유로 그렇게 했는지 충분히 설명한다
  → 사용자는 코드를 직접 보지 않으므로, 코드 안에서 무슨 일이 벌어지고 있는지 알기 쉽게 풀어서 말한다

### 에이전트

**투입 판단**
Claude는 사용자 요청의 중요도와 무게를 파악한 뒤, 병렬 처리로 더 빠르게 진행할 수 있거나, 토론·리뷰로 더 높은 퀄리티를 낼 수 있거나, 작업 범위가 커서 분담이 효율적이라고 판단되면 — 사용자가 먼저 얘기하지 않더라도 에이전트 추가 투입을 요청할 수 있다.

**라운드 기반 계획**
에이전트 투입 시, 1라운드씩 끊어서 계획을 먼저 제안한다. 1라운드 = 하나의 독립된 작업 단위 (기획, 구현, 리뷰는 각각 별도 라운드). 예:
- "기획 1라운드: Opus 3개와 작업하겠습니다."
- "개발 라운드: Opus 2개 + Sonnet 5개와 작업하겠습니다."
- "리뷰 라운드: Sonnet 5개와 작업하겠습니다."

사용자 승인 후 해당 라운드를 진행한다.

**모델 구성 상한**
- 논의·기획·중요한 설계/기획 문서 작성: 1라운드당 Opus 최대 5개
- 코드 작성·전체 코드 전수 조사 등: 1라운드당 Opus 최대 5개 + Sonnet 최대 10개
- Haiku는 어떤 경우에도 사용하지 않는다

**에이전트 권한**
- 사용자 승인 없이 자체 투입한 에이전트 (리서치·분석 목적): 읽기 전용. 파일 수정, 커밋, 빌드 등 쓰기 작업은 금지
- 사용자 승인을 받고 투입한 작업 에이전트: 읽기·쓰기 전 권한 허용

### 날짜/연도 인지
연도가 필요한 모든 상황에서 모델 학습 데이터가 아닌 시스템 주입 currentDate를 기준으로 삼는다

### 리서치 정리 규칙

#### 폴더 구조
- 리서치 결과는 `Research/{주제명}/` 폴더에 정리
- `Research/INDEX.md`에 전체 주제 목록과 링크 유지

#### 주제 폴더 내 파일 구성
- **SUMMARY.md**: 리서치 핵심 결론 1페이지 요약
- **리서치 파일**: 내용 분량에 따라 1개~여러 개로 분할. 참고 링크 포함

#### 작성 시점
- **Q&A 응답이 끝나면 자동으로 정리** (사용자가 별도로 요청하지 않아도 됨)
- 정리 대상: 새로 생성된 리서치 파일, INDEX.md 업데이트, SUMMARY.md 갱신
- 주제 전환 시 CONTEXT.md도 갱신

### CONTEXT.md 관리 규칙
- 역할: 프로젝트 전체 맥락을 한눈에 파악할 수 있는 대시보드
- **100줄 미만 유지** (80줄 넘으면 정리)
- 섹션: "핵심 발견", "현재 방향", "미해결 질문"
- 추가 기준: 여러 주제에 걸쳐 반복적으로 중요한 것만
- 삭제 기준: 후속 발견으로 대체된 내용, 탐구 중단 방향, 이미 리서치 파일에 상세 정리된 것
- 특정 주제에만 해당하는 내용은 해당 주제의 SUMMARY.md에 남길 것
